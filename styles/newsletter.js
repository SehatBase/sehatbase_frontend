import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme)=>{
  return {
    pageContainer:{
        display:"flex",
        backgroundColor:"#FFFFFF",
        padding:"5%",
    },

}})

export { useStyles };
